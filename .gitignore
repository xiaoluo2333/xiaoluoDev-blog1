# =====================================================
# 喵～ 这些文件/目录不需要提交到 Git 仓库哦！
# 如果不小心把敏感数据或编译产物提交上去，会非常麻烦的喵！
# =====================================================

# ==================== Java / Maven 编译输出 ====================
# 喵～ target/ 是 Maven 编译生成的目录，每个人本地编译结果都不一样，不用提交喵
target/
# 喵～ .class 文件是 Java 源码编译后的字节码，由 target/ 统一管理
*.class
# 喵～ .jar 是打包后的文件，除非是插件目录下的特有 JAR，否则不提交
*.jar
# 注意：plugins/*.jar 例外，这个在后面用 !plugins/*.jar 排除掉了
!plugins/*.jar

# ==================== Node.js 依赖 ====================
# 喵～ node_modules 辣么大，提交了会被打的！(>_<)
node_modules/
frontend/node_modules/

# ==================== IDE 相关文件 ====================
# 喵～ 每个人的 IDE 配置都不一样，不要互相污染喵
.idea/
*.iml
.vscode/
# 喵～ vim/neovim 的临时交换文件，关掉编辑器就删掉的东西
*.swp
*.swo

# ==================== 操作系统垃圾文件 ====================
# 喵～ 这些都是 Finder / 资源管理器自动生成的辣鸡文件
.DS_Store
Thumbs.db

# ==================== 运行时生成的文件和目录 ====================
# 喵～ 用户上传的文件、下载的缓存，还有运行时解压/生成的插件，都不该进仓库
uploads/
downloads/
plugins/

# ==================== 日志文件 ====================
# 喵～ 日志天天变，而且可能包含调试信息，不提交最好
*.log

# ==================== 外部配置（敏感信息警告！） ====================
# 喵～ 注意啦！config/blog.yml 里面包含了数据库密码、JWT 密钥等敏感信息！
# 每个人本地部署时的配置都不一样，千万不要提交上去，不然账号安全就危险了喵！
config/blog.yml

# ==================== Maven 本地构建产物 ====================
# 喵～ hello-world-plugin 是示例插件，它的 target/ 也是本地的编译产物
hello-world-plugin/target/